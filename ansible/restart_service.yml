---
- name: Restart a specific service
  hosts: exchangeops
  become: yes
  tasks:
    - name: Restart service {{ service_name }}
      ansible.builtin.systemd:
        name: "{{ service_name }}"
        state: restarted
